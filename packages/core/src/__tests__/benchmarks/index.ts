/**
 * Performance Benchmarks Index
 *
 * Central export for all benchmark utilities and suites.
 */

export * from './benchmark-utils';

// Note: Individual benchmark files are test files that run via vitest
// Use `pnpm benchmark` to run all performance benchmarks
